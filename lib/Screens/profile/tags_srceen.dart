import 'package:flutter/material.dart';

class TagsSrceen extends StatelessWidget {
  const TagsSrceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Tags Screen")));
  }
}
